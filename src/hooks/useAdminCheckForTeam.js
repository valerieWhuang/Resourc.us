import {useState} from 'react';
import {useUserContext} from '../StateProvider';

const useAdminCheckForTeam = (adminsList) => {
  // const { user } = useUserContext();
  // const {
  //   id
  // } = user.user;
  // const [isAdmin, setIsAdmin] = useState(false); 
  
  // if (!adminsList) setIsAdmin(false);
  // if (adminsList?.includes[id]) setIsAdmin(true);

  // return {
  //   isAdmin
  // };
  return false;
};

export {useAdminCheckForTeam};